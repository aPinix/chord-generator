// the values have to match the values in the tailwind config (/styles/globals.css)
export const THEMES = [
  { name: 'Default', value: 'default' },
  // { name: 'Alternative', value: 'alternative' },
  // { name: 'Basic', value: 'basic' },
  // add more here...
] as const;
